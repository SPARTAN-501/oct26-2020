let username: string | boolean | number = 'Alice';
username = 'Bob';
username = false;
username = 5;

let truthy: boolean = false;
let num: number = 5;

// num = 'Bob';

const numbers: (number | string | boolean)[] = [1,2,3];
const strings: string[] = ['hello', 'goodbye'];

numbers.push(6);
numbers.push('hello');
