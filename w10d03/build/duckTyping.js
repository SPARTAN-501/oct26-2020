"use strict";
;
const logIn = (user) => {
    // compare passwords
    return true;
};
const potentialUser = {
    username: 'hush puppy',
    password: 'secret'
};
const valid = logIn(potentialUser);
